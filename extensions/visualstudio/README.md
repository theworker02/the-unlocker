# TheUnlocker Visual Studio Extension Scaffold

This folder is a placeholder for a VSIX that should provide:

- Mod project template
- `mod.json` editor
- Validate/package/sign commands
- Local test runner using `TheUnlocker.Modding.TestHarness`

The implementation should shell out to `unlocker-mod` so CLI and IDE behavior stay consistent.
